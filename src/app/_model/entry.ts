import { Employee } from "./employee";
import { Product } from "./product";

export class Entry {
  quantity: string;
  definition: string;
  employeeId: string;
  productId: string;
  dateAdded: string;
}
